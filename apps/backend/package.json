{
  "name": "backend",
  "private": true,
  "type": "module",
  "module": "./lib/src/index.js",
  "scripts": {
    "dev": "alchemy dev --adopt --app backend",
    "deploy": "alchemy deploy --adopt --app backend",
    "destroy": "alchemy destroy --app backend",
    "check": "tsc --noEmit",
    "test": "bunx vitest",
    "lint": "eslint src/"
  },
  "exports": {
    "./alchemy": {
      "bun": "./alchemy.run.ts",
      "import": "./lib/alchemy.run.js",
      "types": "./alchemy.run.ts"
    }
  },
  "dependencies": {
    "alchemy": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@repo/eslint-config": "catalog:",
    "@repo/typescript-config": "catalog:",
    "typescript": "catalog:",
    "vitest": "~3.2.0",
    "wrangler": "^4.36.0"
  }
}